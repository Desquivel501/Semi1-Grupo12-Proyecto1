import { Album } from "./types";

export class AlbumModel {
  static createAlbum(album: Album) {
  }
  static getAlbum({ id }: { id: number }) {
  }
  static editAlbum({ data }: { data: any }) {
  }
  static deleteAlbum({ id }: { id: number }) {
  }
}
