import { Artist } from "./types";

export class ArtistModel {
  static createArtist(artist: Artist) {
  }
  static getArtist({ id }: { id: number }) {
  }
  static editArtist({ data }: { data: any }) {
  }
  static deleteArtist({ id }: { id: number }) {
  }
}
